nick = "ispolin"
username = "ispolin"
realname = "ispolin"

prefix = "."

servers = {
	{
		host = "crimson.lostsig.net",
		owner = "tm512@crimson.lostsig.net",
		-- Use the following if you want the bot to log into services upon connect
		-- ns_nick = "NickServ"
		-- ns_command = "identify nick password"
		channels = {
			"#bottest"
		}
	}
}

-- modules = { "linktitle.so", "seen.so" }
